# MangroveMarkets Status Report

## Current State

- The application builds and runs locally.
- Nothing is deployed.
- Deployment is still needed in GCP project `mangrove-markets`.
- The GoDaddy domain `mangrovemarkets.com` needs to be mapped to the Cloud Run service.
